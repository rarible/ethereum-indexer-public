spring:
  cloud:
    service-registry:
      auto-registration:
        enabled: false
    discovery:
      enabled: false
    consul:
      config:
        enabled: false
rarible:
  common:
    elasticPrefix: dev_
    elasticNode: localhost:9200
  core:
    apm:
      agent:
        enabled: false

mongoUrls: localhost
mongoDatabase: rarible