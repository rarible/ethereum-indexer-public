spring:
  data:
    mongodb:
      uri: mongodb://${mongodb.hosts}
      database: ${mongodb.database}
rarible:
  core:
    ethereum:
      url: ${node.hosts}
      web-socke-turl: ${node.webSocketHosts}
